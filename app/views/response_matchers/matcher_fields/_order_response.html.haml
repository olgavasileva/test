= f.label "Top choice"
.clear
= f.collection_radio_buttons(:first_place_choice_id, f.object.question.choices, :id, :title, item_wrapper_class: :horizontal) do |choice|
  = choice.label { choice.radio_button + image_tag(choice.object.background_image.web_image_url)  }