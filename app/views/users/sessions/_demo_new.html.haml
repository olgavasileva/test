= simple_form_for resource, as: resource_name, url: session_path(resource_name) do |f|
  .panel.panel-default
    .panel-heading
      .panel-title
        .row
          .col-xs-3.col-sm-2
            =link_to welcome_path do
              %span.glyphicon.glyphicon-chevron-left
    .panel-body.response
      .row
        .col-sm-offset-4.col-sm-4
          %h2.text-center Sign In
          = f.input :login, autofocus: true, placeholder:"Enter your username or email"
          = f.input :password, autocomplete: "off", placeholder: "Enter your password"
          %br
          .text-center
            = f.submit "Next", class:%w(btn btn-default btn-lg btn-block)
          %br
